
function conversor() {
    let dist = Number(prompt("Digite uma distância em metros"))

    let res = document.getElementById("resultado")
    res.innerHTML = `A distância de ${dist} metros, corresponde a...`
    res.innerHTML += `${Math.}`
}